(()=>{const status=document.querySelector('[data-stats-status]'),cards=document.querySelector('[data-stats-cards]'),panels=document.querySelector('[data-stats-panels]'),generated=document.querySelector('[data-stats-generated]');const fields={total:document.querySelector('[data-stat-total]'),installer:document.querySelector('[data-stat-installer]'),portable:document.querySelector('[data-stat-portable]'),last7Days:document.querySelector('[data-stat-seven]'),last30Days:document.querySelector('[data-stat-thirty]')};const versionRows=document.querySelector('[data-version-rows]'),dailyRows=document.querySelector('[data-daily-rows]');const fmt=new Intl.NumberFormat('en-GB');function cell(value){const td=document.createElement('td');td.textContent=value;return td;}function addRows(target,rows,mapper,emptyText){target.replaceChildren();if(!rows.length){const tr=document.createElement('tr'),td=cell(emptyText);td.colSpan=3;td.className='stats-empty';tr.append(td);target.append(tr);return;}for(const row of rows){const tr=document.createElement('tr');for(const value of mapper(row))tr.append(cell(value));target.append(tr);}}async function load(){try{const r=await fetch('/api/download-stats',{cache:'no-store',headers:{Accept:'application/json'}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`Statistics API returned ${r.status}`);for(const [key,node] of Object.entries(fields))if(node)node.textContent=fmt.format(Number(d[key]||0));addRows(versionRows,d.byVersion||[],row=>[row.version,row.package==='portable'?'Portable ZIP':'Installer',fmt.format(Number(row.downloads||0))],'No downloads recorded yet.');addRows(dailyRows,d.daily||[],row=>[row.date,row.package==='portable'?'Portable ZIP':'Installer',fmt.format(Number(row.downloads||0))],'No downloads recorded in the last 30 days.');if(status){status.textContent='Download tracking is active.';status.classList.add('ok');}if(cards)cards.hidden=false;if(panels)panels.hidden=false;if(generated&&d.generatedAt)generated.textContent=`Updated ${new Date(d.generatedAt).toLocaleString('en-GB',{dateStyle:'medium',timeStyle:'short'})}`;}catch(error){console.error('[SGJ] Download statistics unavailable',error);if(status){status.textContent=`Download statistics unavailable: ${error.message}`;status.classList.add('error');}}}load();})();