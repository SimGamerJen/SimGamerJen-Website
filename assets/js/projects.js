(async()=>{const grid=document.querySelector('[data-project-list]');if(!grid)return;try{const response=await fetch('/assets/data/projects.json',{cache:'no-store'});if(!response.ok)throw new Error(String(response.status));const data=await response.json();grid.innerHTML='';(data.items||[]).forEach(item=>{const article=document.createElement('article');const status=document.createElement('span');status.className='status';status.textContent=item.status||'Project';const title=document.createElement('h3');title.textContent=item.title||'Untitled project';const description=document.createElement('p');description.textContent=item.description||'';article.append(status,title,description);if(item.url){const link=document.createElement('a');link.href=item.url;link.textContent=item.cta||'Open project ↗';if(/^https?:\/\//i.test(item.url)){link.target='_blank';link.rel='noopener noreferrer';}article.appendChild(link);}grid.appendChild(article);});}catch(error){grid.innerHTML='<article><span class="status">Projects</span><h3>Project list unavailable</h3><p>The live project catalogue could not be loaded just now.</p></article>';}})();