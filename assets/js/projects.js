(async()=>{const grid=document.querySelector('[data-project-list]');if(!grid)return;const safeUrl=value=>{if(!value)return'';try{const u=new URL(value,window.location.origin);return u.protocol==='http:'||u.protocol==='https:'?u.href:'';}catch{return'';}};try{const response=await fetch('/assets/data/projects.json',{cache:'no-store'});if(!response.ok)throw new Error(String(response.status));const data=await response.json();grid.replaceChildren();(data.items||[]).forEach(item=>{const article=document.createElement('article');const status=document.createElement('span');status.className='status';status.textContent=item.status||'Project';const title=document.createElement('h3');title.textContent=item.title||'Untitled project';const description=document.createElement('p');description.textContent=item.description||'';article.append(status,title,description);const url=safeUrl(item.url);if(url){const link=document.createElement('a');link.href=url;link.textContent=item.cta||'Open project ↗';if(new URL(url).origin!==window.location.origin){link.target='_blank';link.rel='noopener noreferrer';}article.appendChild(link);}grid.appendChild(article);});}catch(error){const article=document.createElement('article');const status=document.createElement('span');status.className='status';status.textContent='Projects';const title=document.createElement('h3');title.textContent='Project list unavailable';const text=document.createElement('p');text.textContent='The live project catalogue could not be loaded just now.';article.append(status,title,text);grid.replaceChildren(article);}})();