(async()=>{const root=document.querySelector('[data-watch-featured]');if(!root)return;try{const response=await fetch('/assets/data/watch-featured.json',{cache:'no-store'});if(!response.ok)throw new Error(String(response.status));const data=await response.json();const eyebrow=root.querySelector('[data-featured-eyebrow]');const heading=root.querySelector('[data-featured-heading]');const intro=root.querySelector('[data-featured-intro]');const grid=root.querySelector('[data-featured-grid]');if(eyebrow)eyebrow.textContent=data.eyebrow||'Featured';if(heading)heading.textContent=data.heading||'Featured on SGJ';if(intro)intro.textContent=data.intro||'';grid.innerHTML='';(data.items||[]).forEach(item=>{const article=document.createElement(item.url?'a':'article');article.className=`series-card series-${item.tone||'default'}`;if(item.url){article.href=item.url;if(/^https?:\/\//i.test(item.url)){article.target='_blank';article.rel='noopener noreferrer';}}article.innerHTML=`<div class="series-art"><span class="series-code"></span><span class="series-art-label"></span></div><div class="series-copy"><span class="status"></span><h3></h3><p></p>${item.url?'<span class="series-cta"></span>':''}</div>`;article.querySelector('.series-code').textContent=item.code||'SGJ';article.querySelector('.series-art-label').textContent=item.artLabel||'';article.querySelector('.status').textContent=item.label||'';article.querySelector('h3').textContent=item.title||'';article.querySelector('p').textContent=item.description||'';const cta=article.querySelector('.series-cta');if(cta)cta.textContent=item.cta||'View →';grid.appendChild(article);});}catch(error){root.hidden=true;}})();